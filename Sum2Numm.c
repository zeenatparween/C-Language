#include <stdio.h>
#include <conio.h>

int main()
{
    int a, b;
    printf("Enter value of a and b: ");
    scanf("%d%d", &a, &b);
    int sum = a + b;
    printf("Sum is: %d", sum);
    return 0;
}